{
  "rounds": 5,
  "variantsPerRound": 32,
  "survivors": 6,
  "wildcards": 1,

  "proposalSplit": { "anthropic": 8, "xai": 8, "openai": 8, "mechanical": 8 },
  "mutation": { "keysPerChild": [2, 4], "sigmaPctOfRange": 12 },

  "judging": {
    "pairsPerVariantPerJudge": 3,
    "extraRandomPairsPerVariant": 1,
    "shuffleSlots": true,
    "maxWords": 25,
    "imageLongestSide": 768,
    "imageFormat": "jpeg",
    "imageQuality": 80,
    "useBatchApi": true
  },

  "elo": { "start": 1500, "k": 24, "applyAtEndOfRound": true },

  "models": {
    "anthropic": { "generator": "claude-opus-5", "judge": "claude-sonnet-5" },
    "xai": { "base_url": "https://api.x.ai/v1", "generator": "grok-4.6", "judge": "grok-4.6" },
    "openai": { "generator": "gpt-5.1", "judge": "gpt-5.4-mini" }
  },

  "limits": {
    "maxUsd": 12,
    "maxMinutes": 45,
    "retriesPerCall": 1,
    "abortOnCapAndWriteWhatExists": true
  },

  "note": "Model ids and prices move. Verify against platform.claude.com/docs pricing, docs.x.ai, and platform.openai.com/docs/pricing before the first paid shift, and re-run the dry run (variantsPerRound 4, rounds 1, one judge) whenever a model id changes. anthropic's prices in src/syndicate/cost.js are still UNVERIFIED — check before real spend. openai.generator/judge (gpt-5.1 / gpt-5.4-mini) and their prices were confirmed live against the API and platform.openai.com/docs/pricing on 2026-08-17; gpt-5.1 has no mini sibling, so the judge deliberately runs a different generation than the generator. Three symmetric vendors (was two) means judging costs roughly 1.5x what it did per pair; the maxUsd cap below is unchanged from before that decision — raise it deliberately if you want a full shift to finish without hitting it."
}
