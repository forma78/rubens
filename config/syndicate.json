{
  "rounds": 1,
  "variantsPerRound": 32,
  "survivors": 6,
  "wildcards": 1,
  "proposalSplit": {
    "anthropic": 8,
    "xai": 8,
    "openai": 8,
    "mechanical": 8
  },
  "mutation": {
    "keysPerChild": [
      2,
      4
    ],
    "sigmaPctOfRange": 12
  },
  "judging": {
    "pairsPerVariantPerJudge": 3,
    "extraRandomPairsPerVariant": 1,
    "shuffleSlots": true,
    "maxWords": 25,
    "imageLongestSide": 768,
    "imageFormat": "jpeg",
    "imageQuality": 80,
    "useBatchApi": false,
    "streamEvery": 16
  },
  "screening": {
    "enabled": false,
    "finalists": 8,
    "minHammingDistance": 3,
    "tilePx": 512,
    "cols": 4,
    "maxTilesPerSheet": 12
  },
  "elo": {
    "start": 1500,
    "k": 24,
    "applyAtEndOfRound": true
  },
  "models": {
    "anthropic": {
      "generator": "claude-opus-5",
      "judge": "claude-sonnet-5"
    },
    "xai": {
      "base_url": "https://api.x.ai/v1",
      "generator": "grok-4.3",
      "judge": "grok-4.3"
    },
    "openai": {
      "generator": "gpt-5.1",
      "judge": "gpt-5.4-mini"
    }
  },
  "limits": {
    "maxUsd": 12,
    "maxMinutes": 45,
    "retriesPerCall": 1,
    "abortOnCapAndWriteWhatExists": true,
    "concurrency": {
      "propose": 12,
      "render": 6,
      "judge": 24
    }
  },
  "note": "screening.enabled is false: flipping it on and running a real 12-variant round (2026-08-19) crashed judgeRound — dedupe's aHash was greyscale-only and collapsed 11 of 12 genuinely different real proposals to Hamming distance 0-2 from each other, leaving 1 survivor for a pairwise tournament that needs at least pairsPerVariantPerJudge+1. Fixed aHash to threshold R/G/B separately (192 bits, not 64) instead of greyscale; minHammingDistance recalibrated to 3 against that same real round's actual hash distances (true duplicates — variants whose patch got rejected and rendered as an unchanged copy of the parent — sat at exactly 0; the least different real, accepted variants started around 3). That's one 12-variant sample on one canvas format, and two real re-runs after the fix both completed cleanly — but both also deduped down to <= finalists, so screenRound's early exit skipped the contact-sheet stage (the round-N/screen-calls.jsonl entry reads {skipped:true}) both times. The dedupe half is real-verified; the contact-sheet half (an actual vendor call per sheet/role/vendor, real Borda voting) has only ever run against fake clients (test/screen.test.js). Needs one shift where variantsPerRound clears finalists after dedupe before flipping this on for good. useBatchApi false is the live shift: ordinary endpoints, limits.concurrency.judge calls in flight, a round in seconds. Set it true only for an unattended volume run where the batch discount is worth a queue with a 24h SLA. limits.concurrency.judge is the one number to lower if a vendor starts returning 429s. Model ids and prices move. Verify against platform.claude.com/docs pricing, docs.x.ai, and platform.openai.com/docs/pricing before the first paid shift, and re-run the dry run (variantsPerRound 4, rounds 1, one judge) whenever a model id changes. openai.generator/judge (gpt-5.1 / gpt-5.4-mini) and their prices were confirmed live against the API and platform.openai.com/docs/pricing on 2026-08-17; gpt-5.1 has no mini sibling, so the judge deliberately runs a different generation than the generator. JUDGING MODELS ARE NO LONGER SET HERE (2026-08-21): each judge in config/roles.json names its own `vendor` and `model`, six judges two per vendor, one call per pair per judge instead of four personas replayed across three vendors. models.<vendor>.judge below is only the fallback for a judge that names no model of its own; models.<vendor>.generator is still live. Measured against the real per-call token counts of the 2026-08-19 shift, the six pinned judges cost about $0.83 a round against $1.18 for the old twelve calls — 30% less even with Opus 5 among them, because six calls beat twelve. Every one of the six ids was listed live on its vendor's own API on 2026-08-21 and sent a real image to confirm it can see; prices in src/syndicate/cost.js were re-checked the same day and none of them is UNVERIFIED any more (Opus 5 had been sitting at three times its real price). One thing left unmeasured: grok-4.6's cost above is grok-4.3's real token counts at 4.6's prices, and if 4.6 spends more reasoning tokens on the same short task it will come out higher — the first real shift's cost log settles it."
}
